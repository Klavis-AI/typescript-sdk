/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments.js";
import * as core from "../../../../core/index.js";
import * as Klavis from "../../../index.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import urlJoin from "url-join";
import * as errors from "../../../../errors/index.js";

export declare namespace CloseOauth {
    export interface Options {
        environment?: core.Supplier<environments.KlavisEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        apiKey?: core.Supplier<core.BearerToken | undefined>;
        /** Additional headers to include in requests. */
        headers?: Record<string, string | core.Supplier<string | undefined> | undefined>;
        fetcher?: core.FetchFunction;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string | core.Supplier<string | undefined> | undefined>;
    }
}

export class CloseOauth {
    protected readonly _options: CloseOauth.Options;

    constructor(_options: CloseOauth.Options = {}) {
        this._options = _options;
    }

    /**
     * Start Close OAuth flow
     *
     * Parameters:
     * - instance_id: Identifier for the instance requesting authorization
     * - client_id: Optional client ID for white labeling
     * - scope: Optional scopes to request (comma-separated)
     * - redirect_url: Optional URL to redirect to after authorization completes
     *
     * @param {Klavis.AuthorizeCloseRequest} request
     * @param {CloseOauth.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Klavis.UnprocessableEntityError}
     *
     * @example
     *     await client.closeOauth.authorizeClose({
     *         instance_id: "instance_id"
     *     })
     */
    public authorizeClose(
        request: Klavis.AuthorizeCloseRequest,
        requestOptions?: CloseOauth.RequestOptions,
    ): core.HttpResponsePromise<unknown> {
        return core.HttpResponsePromise.fromPromise(this.__authorizeClose(request, requestOptions));
    }

    private async __authorizeClose(
        request: Klavis.AuthorizeCloseRequest,
        requestOptions?: CloseOauth.RequestOptions,
    ): Promise<core.WithRawResponse<unknown>> {
        const { instance_id: instanceId, client_id: clientId, scope, redirect_url: redirectUrl } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        _queryParams["instance_id"] = instanceId;
        if (clientId != null) {
            _queryParams["client_id"] = clientId;
        }

        if (scope != null) {
            _queryParams["scope"] = scope;
        }

        if (redirectUrl != null) {
            _queryParams["redirect_url"] = redirectUrl;
        }

        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: urlJoin(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.KlavisEnvironment.Default,
                "oauth/close/authorize",
            ),
            method: "GET",
            headers: mergeHeaders(
                this._options?.headers,
                mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
                requestOptions?.headers,
            ),
            queryParameters: _queryParams,
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Klavis.UnprocessableEntityError(
                        _response.error.body as Klavis.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.KlavisError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.KlavisError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.KlavisTimeoutError("Timeout exceeded when calling GET /oauth/close/authorize.");
            case "unknown":
                throw new errors.KlavisError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Handles the callback from Close OAuth authorization.
     *
     * @param {Klavis.CloseOAuthCallbackRequest} request
     * @param {CloseOauth.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Klavis.BadRequestError}
     * @throws {@link Klavis.UnprocessableEntityError}
     *
     * @example
     *     await client.closeOauth.closeOAuthCallback()
     */
    public closeOAuthCallback(
        request: Klavis.CloseOAuthCallbackRequest = {},
        requestOptions?: CloseOauth.RequestOptions,
    ): core.HttpResponsePromise<Klavis.CloseOAuthSuccessResponse> {
        return core.HttpResponsePromise.fromPromise(this.__closeOAuthCallback(request, requestOptions));
    }

    private async __closeOAuthCallback(
        request: Klavis.CloseOAuthCallbackRequest = {},
        requestOptions?: CloseOauth.RequestOptions,
    ): Promise<core.WithRawResponse<Klavis.CloseOAuthSuccessResponse>> {
        const { code, state, error } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (code != null) {
            _queryParams["code"] = code;
        }

        if (state != null) {
            _queryParams["state"] = state;
        }

        if (error != null) {
            _queryParams["error"] = error;
        }

        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: urlJoin(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.KlavisEnvironment.Default,
                "oauth/close/callback",
            ),
            method: "GET",
            headers: mergeHeaders(
                this._options?.headers,
                mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
                requestOptions?.headers,
            ),
            queryParameters: _queryParams,
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Klavis.CloseOAuthSuccessResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Klavis.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new Klavis.UnprocessableEntityError(
                        _response.error.body as Klavis.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.KlavisError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.KlavisError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.KlavisTimeoutError("Timeout exceeded when calling GET /oauth/close/callback.");
            case "unknown":
                throw new errors.KlavisError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Refresh an expired Close access token using the stored refresh token
     *
     * Parameters:
     * - instance_id: Identifier for the instance requesting token refresh
     *
     * Returns:
     * - Success response if token was refreshed successfully, error response otherwise
     *
     * @param {Klavis.RefreshCloseTokenOauthCloseRefreshTokenPostRequest} request
     * @param {CloseOauth.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Klavis.BadRequestError}
     * @throws {@link Klavis.UnprocessableEntityError}
     *
     * @example
     *     await client.closeOauth.refreshCloseToken({
     *         instance_id: "instance_id"
     *     })
     */
    public refreshCloseToken(
        request: Klavis.RefreshCloseTokenOauthCloseRefreshTokenPostRequest,
        requestOptions?: CloseOauth.RequestOptions,
    ): core.HttpResponsePromise<Klavis.CloseOAuthSuccessResponse> {
        return core.HttpResponsePromise.fromPromise(this.__refreshCloseToken(request, requestOptions));
    }

    private async __refreshCloseToken(
        request: Klavis.RefreshCloseTokenOauthCloseRefreshTokenPostRequest,
        requestOptions?: CloseOauth.RequestOptions,
    ): Promise<core.WithRawResponse<Klavis.CloseOAuthSuccessResponse>> {
        const { instance_id: instanceId } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        _queryParams["instance_id"] = instanceId;
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: urlJoin(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.KlavisEnvironment.Default,
                "oauth/close/refresh_token",
            ),
            method: "POST",
            headers: mergeHeaders(
                this._options?.headers,
                mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
                requestOptions?.headers,
            ),
            queryParameters: _queryParams,
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Klavis.CloseOAuthSuccessResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Klavis.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new Klavis.UnprocessableEntityError(
                        _response.error.body as Klavis.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.KlavisError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.KlavisError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.KlavisTimeoutError("Timeout exceeded when calling POST /oauth/close/refresh_token.");
            case "unknown":
                throw new errors.KlavisError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Revoke Close access and refresh tokens
     *
     * Parameters:
     * - instance_id: Identifier for the instance requesting token revocation
     *
     * Returns:
     * - Success response if token was revoked successfully, error response otherwise
     *
     * @param {Klavis.RevokeCloseTokenOauthCloseRevokePostRequest} request
     * @param {CloseOauth.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Klavis.BadRequestError}
     * @throws {@link Klavis.UnprocessableEntityError}
     *
     * @example
     *     await client.closeOauth.revokeCloseToken({
     *         instance_id: "instance_id"
     *     })
     */
    public revokeCloseToken(
        request: Klavis.RevokeCloseTokenOauthCloseRevokePostRequest,
        requestOptions?: CloseOauth.RequestOptions,
    ): core.HttpResponsePromise<Klavis.CloseOAuthSuccessResponse> {
        return core.HttpResponsePromise.fromPromise(this.__revokeCloseToken(request, requestOptions));
    }

    private async __revokeCloseToken(
        request: Klavis.RevokeCloseTokenOauthCloseRevokePostRequest,
        requestOptions?: CloseOauth.RequestOptions,
    ): Promise<core.WithRawResponse<Klavis.CloseOAuthSuccessResponse>> {
        const { instance_id: instanceId } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        _queryParams["instance_id"] = instanceId;
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: urlJoin(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.KlavisEnvironment.Default,
                "oauth/close/revoke",
            ),
            method: "POST",
            headers: mergeHeaders(
                this._options?.headers,
                mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
                requestOptions?.headers,
            ),
            queryParameters: _queryParams,
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as Klavis.CloseOAuthSuccessResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Klavis.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new Klavis.UnprocessableEntityError(
                        _response.error.body as Klavis.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.KlavisError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.KlavisError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.KlavisTimeoutError("Timeout exceeded when calling POST /oauth/close/revoke.");
            case "unknown":
                throw new errors.KlavisError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getAuthorizationHeader(): Promise<string | undefined> {
        const bearer = await core.Supplier.get(this._options.apiKey);
        if (bearer != null) {
            return `Bearer ${bearer}`;
        }

        return undefined;
    }
}
